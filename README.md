# dotfiles

## Installation

Ensure the `git` and `curl` are installed. Then clone the repo in the `$HOME` directory, `cd` in the repo and execute the installation script:

```
git clone git@github.com:abc-valera/dotfiles.git
cd dotfiles
./install.sh
```
